{% extends 'base.html.twig' %}

{% block title %}CategorieCollect{% endblock %}

{% block body %}
    <h1>CategorieCollect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_collect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_collect.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_collect.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_collect_index') }}">back to list</a>

    <a href="{{ path('app_categorie_collect_edit', {'id': categorie_collect.id}) }}">edit</a>

    {{ include('categorie_collect/_delete_form.html.twig') }}
{% endblock %}
