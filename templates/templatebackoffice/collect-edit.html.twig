{% extends 'templatebackoffice/index.html.twig' %}

{% block title %}Edit Collect{% endblock %}

{% block body %}
    <h1>Edit Collect</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Save Changes</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_collect_management') }}" class="btn btn-secondary">Back to List</a>
{% endblock %}