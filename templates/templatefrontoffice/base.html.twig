<!DOCTYPE html>
<html>

<head>
  <!-- Basic -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <!-- Mobile Metas -->
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <!-- Site Metas -->
  <meta name="keywords" content="" />
  <meta name="description" content="" />
  <meta name="author" content="" />
  <link rel="icon" href="{{asset('frontoffice/images/livecycle.jpg')}}" type="">

  <title>{% block title %}LiveCycle{% endblock %}</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" type="text/css" href="{{asset('frontoffice/css/bootstrap.css')}}" />

  <!-- Owl Carousel CSS -->
  <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css" />

  <!-- Font Awesome CSS -->
  <link href="{{asset('frontoffice/css/font-awesome.min.css')}}" rel="stylesheet" />

  <!-- Custom Styles -->
  <link href="{{asset('frontoffice/css/style.css')}}" rel="stylesheet" />
  <link href="{{asset('frontoffice/css/responsive.css')}}" rel="stylesheet" />

  <!-- Custom Styles for This Page -->
  <style>
    .header_section {
      background-color:rgb(33, 75, 39) !important;
      padding: 5px 0;
    }

    .navbar {
      background-color: rgb(33, 75, 39) !important;
    }

    .hero_area {
      position: relative;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('{{asset('frontoffice/images/homebackground.jpg')}}');
      background-size: cover;
      background-position: center;
    }

    .hero_area .detail-box {
      text-align: center;
      color: white;
    }

    .hero_area .detail-box h1 {
      font-size: 4rem;
      font-weight: bold;
      margin-bottom: 20px;
    }

    .hero_area .detail-box p {
      font-size: 1.5rem;
      margin-bottom: 30px;
    }

    .carousel-indicators {
      bottom: 20px;
    }

    .carousel-indicators li {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background-color: rgba(255, 255, 255, 0.5);
      margin: 0 5px;
    }

    .carousel-indicators li.active {
      background-color: white;
    }

    .client_section {
      padding: 60px 0;
      background-color: #f8f9fa;
    }

    .client_section .heading_container {
      margin-bottom: 40px;
    }

    .client_section .heading_container h2 {
      font-size: 2.5rem;
      font-weight: bold;
      color: #333;
    }

    .client_section .box {
      background-color: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    .client_section .box img {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      margin-bottom: 15px;
    }

    .footer_section {
      background-color: rgb(33, 75, 39);
      color: white;
      padding: 20px 0;
    }

    .footer_section a {
      color: white;
    }

    .footer_section .footer-logo {
      font-size: 1.5rem;
      font-weight: bold;
    }

    .footer_section .footer_social a {
      margin-right: 10px;
      font-size: 1.2rem;
    }

    .profile-dropdown {
  position: relative;
  display: inline-block;
}

.profile-image {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  cursor: pointer;
  border: 2px solid white;
  object-fit: cover;
}

.dropdown-menu {
  display: none;
  position: absolute;
  right: 0;
  top: 100%;
  background: white;
  min-width: 120px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
  border-radius: 4px;
  z-index: 1000;
  padding: 0; /* Changed from 0.5rem 0 */
  overflow: hidden; /* Added to contain hover effects */
}

.dropdown-item {
  display: block;
  padding: 0.5rem 1rem;
  color: #333;
  text-decoration: none;
  font-size: 14px;
  transition: all 0.2s ease;
  position: relative;
}

.dropdown-item:hover {
  background-color: #f8f9fa;
  /* Added these to contain the hover effect */
  margin: 0;
  border-radius: 0;
}

/* Optional: Add a subtle separator */
.dropdown-item:not(:last-child) {
  border-bottom: 1px solid #eee;
}

/* If you want the hover effect to match the dropdown's border radius */
.dropdown-item:first-child:hover {
  border-radius: 4px 4px 0 0;
}

.dropdown-item:last-child:hover {
  border-radius: 0 0 4px 4px;
}

    html, body {
      height: 100%;
    }

    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .page-wrapper {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .content-wrapper {
      flex: 1;
      padding-bottom: 60px; /* Adjust based on footer height */
    }

    .footer_section {
      flex-shrink: 0;
      margin-top: auto;
    }
    .dropdown-menu {
      min-width: 200px; /* Increased from 120px */
      padding: 0.5rem 0; /* Added padding */
    }

    .dropdown-item {
      padding: 0.75rem 1.5rem; /* Increased padding */
      font-size: 16px; /* Larger font size */
      color: #333 !important; /* Ensure text color contrast */
      white-space: normal;
      z-index: 1000;/* Allow text wrapping */
    }

    .dropdown-item:hover {
      background-color: #f8f9fa;
      color: #000 !important;
    }

    /* For active state */
    .dropdown-item.active {
      background-color: rgba(33, 75, 39, 0.1);
      color: rgb(33, 75, 39) !important;
      font-weight: 500;
    }

    /* Ensure dropdown aligns properly */
    .navbar-nav .dropdown-menu {
      position: absolute;
      left: auto;
      right: 0;
    }

    .fa-shopping-cart {
      transition: transform 0.3s ease;
    }

    .fa-shopping-cart:hover {
      transform: scale(1.1);
      color: #88c9a1 !important;  {# Accent color on hover #}
    }

    /* Optional: Add cart counter */
    .cart-counter {
      position: absolute;
      top: -3px;       /* Adjusted from -5px */
      right: -5px;     /* Adjusted from -8px */
      background: #ff4d4d;
      color: white;
      border-radius: 50%;
      padding: 1px 4px; /* Reduced from 2px 6px */
      font-size: 0.6rem; /* Reduced from 0.75rem */
      line-height: 1;   /* Added for better vertical alignment */
      z-index: 1002;
    }

    /* In your existing CSS */
    .nav-item.ms-3.me-4.position-relative {
      z-index: 1001; /* Higher than profile dropdown's z-index */
    }


    .collapse.navbar-collapse {
      z-index: 1;
      position: relative;
    }

    .user_option {
      z-index: 1002;
      position: relative;
    }
    .add-to-cart {
      transition: transform 0.2s ease;
    }

    .add-to-cart:active {
      transform: scale(0.95);
    }

  </style>





  {% block stylesheets %}{% endblock %}




</head>






<div class="page-wrapper">


  {% block header %}
    <header class="header_section">
      <div class="container">
        <nav class="navbar navbar-expand-lg custom_nav-container">
          <a class="navbar-brand" href="/templatefrontoffice">
            <img src="{{ asset('frontoffice/images/logo_rounded.png') }}"
                 alt="LiveCycle Logo"
                 style="height:55px;width:55px;margin-right:20px;">
            <span>LiveCycle</span>
          </a>
          <button class="navbar-toggler" type="button"
                  data-toggle="collapse" data-target="#navbarSupportedContent"
                  aria-controls="navbarSupportedContent" aria-expanded="false"
                  aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          {# ← your `active` block wraps exactly the collapse/nav-items #}
          {% block active %}
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav mx-auto">
                <li class="nav-item active">
                  <a class="nav-link" href="/templatefrontoffice">HOME</a>
                </li>
                <li class="nav-item dropdown {{ 'annonce' in app.request.get('_route') ? 'active' : '' }}">
                  <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
                    ANNONCE
                  </a>
                  <div class="dropdown-menu">
                    {% if app.user and 'ROLE_ENTREPRISE' not in app.user.roles %}
                      <a class="dropdown-item" href="/templatefrontoffice/annonce/myAnnoncements">
                        My Announcements
                      </a>
                    {% endif %}
                    <a class="dropdown-item" href="/templatefrontoffice/annonce/">Shop Announcement</a>
                  </div>
                </li>
                <li class="nav-item"><a class="nav-link" href="/templatefrontoffice/collect">COLLECT</a></li>
                <li class="nav-item"><a class="nav-link" href="/templatefrontoffice/forum">FORUM</a></li>
                <li class="nav-item"><a class="nav-link" href="/templatefrontoffice/reclamation">COMMANDES</a></li>
                <li class="nav-item"><a class="nav-link" href="/templatefrontoffice/reclamation">RECLAMATION</a></li>

                {# Cart icon inside collapse #}
                <li class="nav-item ms-3 me-4 mr-5 position-relative">
                  <a class="nav-link text-white p-0" href="{{ path('cart_view') }}">
                    <i class="fa fa-shopping-cart fs-5"></i>
                    <span class="cart-counter">{{ cart_count() }}</span>
                  </a>
                </li>
              </ul>
            </div>
          {% endblock %}

          {# Profile OUTSIDE collapse, pushed to far right #}
          <div class="user_option ms-auto ml-5">
            {% if app.user %}
              <div class="profile-dropdown">
                <img src="{{ app.user.image
                ? asset('frontoffice/images/' ~ app.user.image)
                : asset('frontoffice/images/default.jpg') }}"
                     class="profile-image"
                     alt="Profile"
                     id="profileDropdownTrigger">
                <div class="dropdown-menu" id="profileDropdown">
                  <a href="{{ path('user_profile_edit') }}" class="dropdown-item">Profil</a>
                  <a href="{{ path('app_logout') }}" class="dropdown-item">Logout</a>
                </div>
              </div>
            {% else %}
              <a href="/login" class="order_online">LOG IN</a>
            {% endif %}
          </div>
        </nav>
      </div>
    </header>
  {% endblock %}











  <div class="content-wrapper">
  {% block body %}
  <!-- Hero Section -->
  <div class="hero_area">
    <div id="customCarousel1" class="carousel slide" data-ride="carousel">
    <!-- Carousel Items -->
    <div class="carousel-inner">
      <!-- Slide 1 -->
      <div class="carousel-item active">
        <div class="container">
          <div class="row">
            <div class="col-md-7 col-lg-6">
              <div class="detail-box">
                <h1>Green Recycle🌱</h1>
                <p>
                  "Thanks to this platform, we were able to access a constant flow of quality recyclable raw materials. A smooth and efficient collaboration!"
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Slide 2 -->
      <div class="carousel-item">
        <div class="container">
          <div class="row">
            <div class="col-md-7 col-lg-6">
              <div class="detail-box">
                <h1>Solutions🌍</h1>
                <p>
                  "Join us in making the world a greener place. Recycle, reuse, and reduce waste with LiveCycle!"
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Slide 3 -->
      <div class="carousel-item">
        <div class="container">
          <div class="row">
            <div class="col-md-7 col-lg-6">
              <div class="detail-box">
                <h1>Sustainable Future🌿</h1>
                <p>
                  "Together, we can build a cleaner, greener, and more sustainable future. Start recycling today with LiveCycle and make a real impact!"
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Slide 4 -->
      <div class="carousel-item">
        <div class="container">
          <div class="row">
            <div class="col-md-7 col-lg-6">
              <div class="detail-box">
                <h1>Community Impact🤝</h1>
                <p>
                  "Our platform connects communities to recycling stakeholders, creating a positive impact on the environment."
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Slide 5 -->
      <div class="carousel-item">
        <div class="container">
          <div class="row">
            <div class="col-md-7 col-lg-6">
              <div class="detail-box">
                <h1>Innovativ Recyclin🚀</h1>
                <p>
                  "Discover innovative solutions for recycling waste. Join LiveCycle today and be part of the change."
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Carousel Indicators -->
    <div class="container mt-5 mr-5">
      <ol class="carousel-indicators">
        <li data-target="#customCarousel1" data-slide-to="0" class="active"></li>
        <li data-target="#customCarousel1" data-slide-to="1"></li>
        <li data-target="#customCarousel1" data-slide-to="2"></li>
        <li data-target="#customCarousel1" data-slide-to="3"></li>
        <li data-target="#customCarousel1" data-slide-to="4"></li>
      </ol>
    </div>
    </div>
  </div>

  <!-- Customer Testimonials Section -->
  <section class="client_section">
    <div class="container">
      <div class="heading_container heading_center">
        <h2>What Our Customers Say</h2>
      </div>
      <div class="row">
        <div class="col-md-6">
          <div class="box">
            <img src="{{asset('frontoffice/images/client1.jpg')}}" alt="Client 1">
            <div class="detail-box">
              <p>
                "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
              </p>
              <h6>Moana Michell</h6>
            </div>
          </div>
        </div>
        <div class="col-md-6">
          <div class="box">
            <img src="{{asset('frontoffice/images/client2.jpg')}}" alt="Client 2">
            <div class="detail-box">
              <p>
                "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
              </p>
              <h6>Mike Hamell</h6>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>



  {% endblock body %}
  </div>



  {% block footer %}


  <footer class="footer_section">
    <div class="container">
      <div class="row">
        <div class="col-md-4">
          <div class="footer_contact">
            <h4>Contact Us</h4>
            <div class="contact_link_box">
              <a href=""><i class="fa fa-map-marker"></i> Location</a>
              <a href=""><i class="fa fa-phone"></i> Call +01 1234567890</a>
              <a href=""><i class="fa fa-envelope"></i> demo@gmail.com</a>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="footer_detail">
            <a href="" class="footer-logo">LiveCycle</a>
            <p>
              "The simple and effective solution to collect, sell, and learn how to recycle your waste."
            </p>
            <div class="footer_social">
              <a href=""><i class="fa fa-facebook"></i></a>
              <a href=""><i class="fa fa-twitter"></i></a>
              <a href=""><i class="fa fa-linkedin"></i></a>
              <a href=""><i class="fa fa-instagram"></i></a>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <h4>Opening Hours</h4>
          <p>Everyday</p>
          <p>10.00 Am - 10.00 Pm</p>
        </div>
      </div>
      <div class="footer-info">
        <p>&copy; <span id="displayYear"></span> All Rights Reserved</p>
      </div>
    </div>
  </footer>




  <!-- Scripts -->
  <script src="{{asset('frontoffice/js/jquery-3.4.1.min.js')}}"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
  <script src="{{asset('frontoffice/js/bootstrap.js')}}"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"></script>
  <script src="{{asset('frontoffice/js/custom.js')}}"></script>


{% endblock footer %}

</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const dropdownTrigger = document.getElementById('profileDropdownTrigger');
  const dropdownMenu = document.getElementById('profileDropdown');

  if (dropdownTrigger && dropdownMenu) {
    // Toggle dropdown on click
    dropdownTrigger.addEventListener('click', function(e) {
      e.stopPropagation();
      dropdownMenu.classList.toggle('show');
    });

    // Close dropdown when clicking outside
    document.addEventListener('click', function(e) {
      if (!dropdownTrigger.contains(e.target)) {
        dropdownMenu.classList.remove('show');
      }
    });
  }
});

document.querySelectorAll('.add-to-cart').forEach(button => {
  button.addEventListener('click', async (e) => {
    e.preventDefault();
    const form = button.closest('form');

    try {
      const response = await fetch(form.action, {
        method: 'POST',
        headers: {
          'X-Requested-With': 'XMLHttpRequest'
        }
      });

      if (response.ok) {
        const cartCounter = document.querySelector('.cart-counter');
        const currentCount = parseInt(cartCounter.textContent);
        cartCounter.textContent = currentCount + 1;

        // Optional: Show flash message
        const flash = document.createElement('div');
        flash.className = 'alert alert-success position-fixed top-0 end-0 m-3';
        flash.textContent = 'Item added to cart!';
        document.body.appendChild(flash);

        setTimeout(() => flash.remove(), 3000);
      }
    } catch (error) {
      console.error('Error:', error);
    }
  });
});



</script>
</html>